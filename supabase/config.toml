project_id = "gameday"

# The handler validates the scheduler's secret API key itself, and also accepts
# an authenticated commissioner request to import the week immediately.
[functions.sports-sync]
verify_jwt = false
