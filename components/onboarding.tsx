import { createPool, redeemInvitation } from "@/app/actions/pool";
import { Button, Card } from "./ui";

export function Onboarding({ pending }: { pending: boolean }) { return <section className="home-hero mx-auto mt-4 max-w-xl overflow-hidden rounded-3xl px-5 py-10 md:mt-10 md:px-8"><p className="eyebrow">Welcome to GameDay</p><h1 className="mt-3 text-4xl font-black">{pending ? "Your membership is pending." : "Start a private pool."}</h1><p className="mt-3 max-w-lg text-sm leading-6 text-slate-200">{pending ? "A commissioner needs to approve your request before picks and standings unlock. We'll email you the moment you're in." : "Create your pool to become its commissioner, or redeem an invitation from your group."}</p>{!pending && <div className="mt-7 grid gap-4 md:grid-cols-2"><Card className="p-5"><h2 className="font-black">Create a pool</h2><form action={createPool} className="mt-4 grid gap-3"><label className="text-xs font-bold text-[hsl(var(--muted))]" htmlFor="name">Pool name</label><input className="focus-ring min-h-11 rounded-xl border bg-black/20 px-3 text-sm" id="name" name="name" required maxLength={100} placeholder="Sunday Crew"/><Button type="submit">Create pool</Button></form></Card><Card className="p-5"><h2 className="font-black">Join a pool</h2><form action={redeemInvitation} className="mt-4 grid gap-3"><label className="text-xs font-bold text-[hsl(var(--muted))]" htmlFor="token">Invitation token</label><input className="focus-ring min-h-11 rounded-xl border bg-black/20 px-3 text-sm" id="token" name="token" required placeholder="Paste token"/><Button type="submit" className="bg-white/10 text-white hover:bg-white/15">Request access</Button></form></Card></div>}</section>; }
