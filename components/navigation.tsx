import Link from "next/link";
import { ChevronDown, Home, LogOut, ShieldCheck, Trophy, UserRound, UsersRound } from "lucide-react";
import { signOut } from "@/app/login/actions";

const tabs = [{ href:"/", label:"Home", icon:Home },{ href:"/pick", label:"Pick", icon:ShieldCheck },{ href:"/picks", label:"Picks", icon:UsersRound },{ href:"/standings", label:"Standings", icon:Trophy },{ href:"/profile", label:"Profile", icon:UserRound }];
export function Header({ weekNumber }: { weekNumber?: number }) { return <header className="mx-auto flex max-w-5xl items-center justify-between px-5 py-5"><Link href="/" className="focus-ring text-xl font-black tracking-tight">GAME<span className="text-[hsl(var(--primary))]">DAY</span></Link><span className="rounded-full bg-white/5 px-3 py-1 text-xs font-bold text-[hsl(var(--muted))]">{weekNumber ? `Week ${weekNumber}` : "NFL Pool"}</span><details className="group relative"><summary className="focus-ring flex min-h-11 cursor-pointer list-none items-center gap-1 rounded-xl px-2 text-xs font-bold text-[hsl(var(--primary))] marker:content-none">Account<ChevronDown size={14} className="transition group-open:rotate-180"/></summary><div className="absolute right-0 z-30 mt-2 w-40 overflow-hidden rounded-xl border border-white/10 bg-[hsl(var(--surface))] p-1 shadow-xl"><Link href="/profile" className="focus-ring flex min-h-10 items-center rounded-lg px-3 text-sm font-bold hover:bg-white/5">Profile</Link><Link href="/admin" className="focus-ring flex min-h-10 items-center rounded-lg px-3 text-sm font-bold hover:bg-white/5">Commissioner</Link><form action={signOut}><button type="submit" className="focus-ring flex min-h-10 w-full items-center gap-2 rounded-lg px-3 text-left text-sm font-bold text-red-200 hover:bg-red-500/10"><LogOut size={15}/>Sign out</button></form></div></details></header>; }
export function BottomNav() { return <nav aria-label="Main navigation" className="fixed inset-x-0 bottom-0 z-20 border-t border-white/10 bg-[hsl(var(--background)/.96)] px-2 pb-[max(.5rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur md:hidden"><div className="mx-auto flex max-w-lg justify-between">{tabs.map(({ href,label,icon:Icon }) => <Link key={href} href={href} className="focus-ring flex min-w-14 flex-col items-center gap-1 rounded-lg px-2 py-1.5 text-[10px] font-bold text-[hsl(var(--muted))] hover:text-[hsl(var(--primary))]"><Icon size={19}/>{label}</Link>)}</div></nav>; }
