"use client"; import { Check, Copy, ExternalLink } from "lucide-react"; import { useState } from "react"; import { Button } from "./ui"; export function OpenInBrowserHelp({ token }: { token: string }) { const [copied, setCopied] = useState(false); const copyInvitation = async () => { const invitationUrl = new URL("/join", window.location.origin); invitationUrl.searchParams.set("token", token); try { await navigator.clipboard.writeText(invitationUrl.toString()); setCopied(true); } catch { window.prompt("Copy this invitation link, then paste it into Safari, Chrome, or Edge:", invitationUrl.toString()); } }; return <div className="mt-6 border-t border-white/10 pt-5 text-left"><p className="flex items-center gap-2 text-sm font-black"><ExternalLink size={16} className="text-[hsl(var(--primary))]"/>Opened this invite from an email app?</p><p className="mt-2 text-sm leading-6 text-[hsl(var(--muted))]">Email apps can open links in their built-in browser. Use the message menu (•••) and choose <strong>Open in Safari</strong> or <strong>Open in browser</strong>. Or copy this invite and paste it into Safari, Chrome, or Edge.</p><div className="mt-3"><Button className="bg-white/10 text-white hover:bg-white/15" onClick={copyInvitation} type="button">{copied ? <Check className="mr-2" size={16}/> : <Copy className="mr-2" size={16}/>}{copied ? "Invite copied" : "Copy invitation link"}</Button></div><p aria-live="polite" className="mt-2 text-xs text-[hsl(var(--muted))]">Your invitation remains private—only share it with the intended player.</p></div>; }
