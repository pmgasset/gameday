import { Header, BottomNav } from "@/components/navigation";
import { Card, Pill } from "@/components/ui";
import { requirePoolContext } from "@/lib/data/pool";
export default async function ProfilePage(){const context=await requirePoolContext(); return <main className="mx-auto min-h-screen max-w-5xl pb-24"><Header/><section className="px-5 pt-6"><p className="eyebrow">Your account</p><h1 className="mt-2 text-3xl font-black">Profile</h1><Card className="mt-6 p-5"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-full bg-[hsl(var(--primary))] font-black text-[hsl(var(--primary-foreground))]">{context.displayName.slice(0,2).toUpperCase()}</div><div><p className="font-black">{context.displayName}</p><p className="text-sm text-[hsl(var(--muted))]">{context.pool ? context.pool.name : context.pendingPool ? "Membership pending" : "No active pool"}</p>{context.pool && <Pill className="mt-2 text-[hsl(var(--primary))]">{context.pool.role.replace("_"," ")}</Pill>}</div></div></Card></section><BottomNav/></main>}
