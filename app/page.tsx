import Link from "next/link";
import { ArrowRight, LockKeyhole, UsersRound } from "lucide-react";
import { Header, BottomNav } from "@/components/navigation";
import { Button, Card, Pill } from "@/components/ui";
import { formatEastern } from "@/lib/domain/deadlines";
import { fixtureGames } from "@/lib/fixtures/week";

export default function HomePage() { const pick = fixtureGames[1]; return <main className="mx-auto min-h-screen max-w-5xl pb-24"><Header/><section className="px-5 pt-7 md:pt-14"><Pill className="border-[hsl(var(--primary)/.4)] text-[hsl(var(--primary))]">NFL Week 7</Pill><h1 className="mt-4 text-4xl font-black tracking-tight md:text-5xl">Sunday starts with<br/><span className="text-[hsl(var(--primary))]">one good call.</span></h1><p className="mt-4 max-w-lg leading-7 text-[hsl(var(--muted))]">Your private NFL underdog pool. Select a team to win outright and follow every result live.</p></section><section className="mt-9 grid gap-4 px-5 md:grid-cols-[1.3fr_.7fr]"><Card className="relative overflow-hidden p-5"><div className="absolute -right-12 -top-12 h-36 w-36 rounded-full bg-[hsl(var(--primary)/.14)] blur-2xl"/><p className="eyebrow">Your pick · Week 7</p><div className="mt-5 flex items-end justify-between"><div><h2 className="text-3xl font-black">Green Bay Packers</h2><p className="mt-1 text-sm text-[hsl(var(--muted))]">at Detroit Lions · {formatEastern(pick.kickoff)}</p></div><span className="text-4xl font-black text-[hsl(var(--primary))]">+3.5</span></div><Link href="/pick" className="mt-6 inline-flex"><Button>Change pick <ArrowRight className="ml-2" size={16}/></Button></Link></Card><Card className="p-5"><div className="flex items-center gap-2 text-[hsl(var(--primary))]"><UsersRound size={18}/><span className="text-sm font-bold">18 of 23 submitted</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full w-[78%] rounded-full bg-[hsl(var(--primary))]"/></div><div className="mt-5 flex gap-3"><LockKeyhole className="mt-0.5 shrink-0 text-[hsl(var(--warning))]" size={18}/><p className="text-sm leading-5 text-[hsl(var(--muted))]">Picks are sealed. Remaining picks reveal Sunday at 1:00 PM ET.</p></div></Card></section><section className="px-5 pt-8"><p className="eyebrow">What happens next</p><div className="mt-3 grid gap-3 md:grid-cols-3">{[["1","Pick one underdog"],["2","Picks reveal at kickoff"],["3","Win outright, earn the spread"]].map(([n,t])=><div key={n} className="flex items-center gap-3 rounded-xl border border-white/10 bg-white/[.025] p-4"><span className="grid h-7 w-7 place-items-center rounded-full bg-[hsl(var(--primary)/.15)] text-xs font-black text-[hsl(var(--primary))]">{n}</span><span className="text-sm font-bold">{t}</span></div>)}</div></section><BottomNav/></main>; }
