import Link from "next/link";
import { Mail, ShieldCheck } from "lucide-react";
import { requestMagicLink } from "./actions";
import { Button, Card } from "@/components/ui";

export default async function LoginPage({ searchParams }: { searchParams: Promise<{sent?:string;error?:string}> }) { const params=await searchParams; return <main className="grid min-h-screen place-items-center px-5"><Card className="w-full max-w-md p-6"><Link className="text-xl font-black" href="/">GAME<span className="text-[hsl(var(--primary))]">DAY</span></Link><h1 className="mt-8 text-3xl font-black">Get in the game.</h1><p className="mt-2 text-sm leading-6 text-[hsl(var(--muted))]">Use your email to receive a secure sign-in link. Private pool access still requires approval.</p>{params.sent ? <div className="mt-6 rounded-xl bg-[hsl(var(--primary)/.12)] p-4 text-sm font-bold text-[hsl(var(--primary))]">Check your email for the sign-in link.</div> : <form action={requestMagicLink} className="mt-7 grid gap-3"><label className="text-sm font-bold" htmlFor="email">Email address</label><input className="focus-ring min-h-11 rounded-xl border bg-black/20 px-3 text-sm" id="email" required type="email" name="email" placeholder="you@example.com"/><Button type="submit"><Mail className="mr-2" size={16}/>Send magic link</Button></form>}{params.error && <p className="mt-4 text-sm text-red-300">We couldn’t send that link. Please try again.</p>}<p className="mt-7 flex gap-2 text-xs leading-5 text-[hsl(var(--muted))]"><ShieldCheck className="shrink-0 text-[hsl(var(--primary))]" size={16}/>GameDay is private. Your invitation becomes a membership request after you sign in.</p></Card></main>; }
