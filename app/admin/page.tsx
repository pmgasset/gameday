import { Activity, ClipboardCheck, Users, UserPlus } from "lucide-react";
import { Header } from "@/components/navigation";
import { Card, Pill } from "@/components/ui";
const stats = [["23","Active players",Users],["18","Picks submitted",ClipboardCheck],["5","Missing picks",UserPlus],["Live","NFL data",Activity]];
export default function AdminPage(){return <main className="mx-auto min-h-screen max-w-5xl pb-12"><Header/><section className="px-5 pt-6"><Pill className="border-[hsl(var(--primary)/.4)] text-[hsl(var(--primary))]">Commissioner area</Pill><h1 className="mt-3 text-3xl font-black">Run Week 7</h1><p className="mt-2 text-sm text-[hsl(var(--muted))]">Pool operations stay separate from the player game.</p><div className="mt-7 grid grid-cols-2 gap-3 md:grid-cols-4">{stats.map(([value,label,Icon])=>{const Symbol=Icon as typeof Users;return <Card className="p-4" key={label as string}><Symbol className="text-[hsl(var(--primary))]" size={18}/><p className="mt-5 text-2xl font-black">{value as string}</p><p className="text-xs font-bold text-[hsl(var(--muted))]">{label as string}</p></Card>})}</div><div className="mt-6 grid gap-3 md:grid-cols-2"><Card className="p-5"><p className="eyebrow">Next deadline</p><p className="mt-2 text-xl font-black">Sunday · 1:00 PM ET</p><p className="mt-2 text-sm text-[hsl(var(--muted))]">All remaining picks lock and reveal.</p></Card><Card className="p-5"><p className="eyebrow">NFL data</p><p className="mt-2 text-xl font-black text-[hsl(var(--primary))]">Connected</p><p className="mt-2 text-sm text-[hsl(var(--muted))]">Last sync: fixture mode</p></Card></div><Card className="mt-6 p-5"><h2 className="font-black">Commissioner tools</h2><div className="mt-4 grid gap-2 text-sm font-bold text-[hsl(var(--muted))] md:grid-cols-3"><span>Members & approvals</span><span>Enter player pick</span><span>Games & spreads</span><span>Invitations</span><span>Audit log</span><span>Provider health</span></div></Card></section></main>}
